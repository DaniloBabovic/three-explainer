/* 
import { setDefaultValue, setMarks, setValue }  from "store/player/playerSlice"
import { setMax, setMin, setStep }              from "store/player/playerSlice" 
*/

export const setDefaultValue = ( defaultValue: number ) => {

    console.log ( defaultValue )
}

export const setMarks = ( marks: {value: number, label: string} [] ) => {

    console.log ( marks )
}

export const setValue = ( value: number ) => {

    console.log ( value )
}

export const setMax = ( max: number ) => {

    console.log ( max )
}

export const setMin = ( min: number ) => {

    console.log ( min )
}

export const setStep = ( step: number ) => {

    console.log ( step )
}